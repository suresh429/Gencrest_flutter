import 'package:flutter/material.dart';

class TsmTeamPage extends StatelessWidget {
  const TsmTeamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
