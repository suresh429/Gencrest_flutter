import 'package:flutter/material.dart';

class TsmAlertScreen extends StatelessWidget {
  const TsmAlertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
